version: "3.8"
services:
  backend:
    build: ./backend
    ports:
      - "8010:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3004:80"
    depends_on:
      - backend
    restart: unless-stopped
